五种I/O模型的理解：
1.blocking IO
	阻塞IO：当一个请求到达时，进程就会被阻塞，系统内核开始准备数据，
	当数据准备完成之后拷贝到进程缓存区，返回结果，这时候用户进程解除block状态，重新运行起来
2.nonblocking IO
	非阻塞IO：当一个请求到达时，如果系统内核数据还没有准备好，这时候并不会阻塞用户进程，
	而是立刻返回一个error。用户判断是一个error时，就说明数据还没准备好，不断轮询检查，当系统内核数据准备好了，
	那么用户进程将数据拷贝到缓冲区，然后返回。
	
3.IO multiplexing
	多路复用IO： 使用了select/epoll函数，会不断轮询所负责的所有socket当某个scoket有数据到达了，就通知用户
	调用select时，整个进程是阻塞的。当任何一个socket中的数据准备好了，select就返回，这时候用户在调用read操作，将数据
	从系统内核拷贝到用户进程。
4.singal driven IO
	信号驱动IO： 当请求到达时，告诉系统内核开始准备数据，然后返回，系统内核准备好数据之后，发送一个信号给用户进程，
	用户进程开始将数据拷贝到缓存区。
5.asynchronous IO
	异步IO： 当请求到达时，告诉系统内核需要数据，然后立即返回，不阻塞进程。系统内核开始准备数据，并将准备好的数据拷贝到
	用户线程，这时通知用户线程，数据已经准备好了

